// // socketThunks.js


// export const initializeSocket = (userInfo) => (dispatch) => {
//     dispatch(connectSocket({ role: userInfo.role, userId: userInfo._id }));
// };

// export const closeSocket = () => (dispatch) => {
//     dispatch(disconnectSocket());
// };
